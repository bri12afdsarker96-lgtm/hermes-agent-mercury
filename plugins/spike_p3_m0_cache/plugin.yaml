name: spike-p3-m0-cache
version: "0.0.1"
description: "P3-M0 Spike · tenant-safe cache + budget orchestrator · registers llm_execution middleware (single seam) · calls sibling spike-p3-m0-budget service · memory-only LRU · fail-closed without tenant context · non-streaming + tool-free + successful terminal responses only · usage split into origin/billable/saved · off by default (opt-in via plugins.enabled)."
author: "Hermes_AI × NousResearch/hermes-agent Spike"
provides_hooks:
  - "llm_execution (middleware)"
